# 改进 Tracker — 2026-05-07 审查回归

来源审查报告: [`AUDIT-2026-05-07.md`](AUDIT-2026-05-07.md)
跟踪格式: 完成项打 `[x]` 并补上 commit / 文件链接；
跨 LLM 复审时，未打勾的项是下一轮关注重点。

---

## 🔴 HIGH (必须在下个版本前关闭)

### [x] H1 — 清除 `command_template` 历史接口残留 ✅ 2026-05-07

- [x] `README.md` 第 504-512 行「External fallback plugins」示例改为
      `argv:` 数组形式 (附 v1.4 breaking change 注释)
- [x] `SKILL.md` (root standalone) 第 77 行 prose 改写: "Each plugin declares
      an `argv:` list (per-token, no shell)"
- [x] `plugin/skills/wiki-query/SKILL.md` 步骤改写: 不再让 agent 自己 render +
      shell out，而是 delegate 给 `external_plugin_run.py`
- [x] `grep -r command_template` 现剩 4 处迁移说明 + 1 处 query 显式 v1.4 警告
      + 拒绝代码 + changelog 历史条目，全部合法

**验收通过:** `grep -ri command_template` 已无活接口示例残留。

### [x] H2 — 多项目 watcher 共存 ✅ 2026-05-07

- [x] `wiki_watch.py` PID 文件名按 wiki root slug 命名
      (`watcher-{leaf}-{sha1[:8]}.pid`)；leaf+hash 应对不同路径同名 leaf
- [x] `start` / `status` / `stop` 都接收 wiki root，挑对应 PID
- [x] 新增 Test 13.5: 两个 fixture 目录验证 pid_file_path 不同 + 一边活
      pid 时另一边 status=not-running

**验收通过:** Test 13.5 通过；既有 Test 13 也保持通过。

### [x] H3 — search 排序: 文档与实现对齐 ✅ 2026-05-07

- [x] `search_naive.py` 接入 `hub_score`:
      `(-title, -tag, -hub, -body, -updated, path)`
- [x] 调用 `build_graph(pages)` 构造 in/out 链路 (复用 wiki_lib)
- [x] Test 11.4 ranking 仍 deterministic (相同输入相同输出)
- [x] `wiki-search/SKILL.md` ⑥ 改成显式 6 维排序，把 hub centrality 写在第 3 位

**验收通过:** smoke 全过；hub centrality 真接入。

### [x] H4 — Standalone SKILL.md 补齐三段 prose ✅ 2026-05-07

- [x] 在 `wiki-import` prose 段后插入 `wiki-watch` / `wiki-dream` /
      `wiki-config` 三段
- [x] 每段含 trigger / args / 关键行为 / 跨项目并存说明 / 安全注意
- [x] `scripts/build_skill_md.py --check` 通过 (autogenerated 表已含 12 项)

**验收通过:** 复制 SKILL.md 给 standalone LLM 现在能调用 12 个 skill 全集。

---

## 🟠 MEDIUM (本周内推进)

### [x] M5 — wiki-init 自带 dreaming 起飞 ✅ 2026-05-07

- [x] `wiki_init.py` 新增 `--enable-dreaming` flag
- [x] 写入 SCHEMA.md 的 dreaming block (默认安全配置: 0.6 阈值, 10 cap,
      180 dormancy, min_count 3)
- [x] 退出消息附带 `claude /schedule "0 23 * * 0" "/kata:wiki-dream"`
- [x] `wiki-init/SKILL.md` 步骤 ④d 新增 dreaming 询问
- [x] **顺手修了一个预存 bug:** 当 `dimensions=[]` 时旧渲染输出
      `custom_dimensions:\n  []` 会被解析成 dict 不是 array，schema_validate
      失败。改为 inline `custom_dimensions: []`，加 Test 16.5 锁定。

**验收通过:** Test 16.5 通过；schedule 建议行已在 init 输出里。

### [x] M6 — dogfood-v1.6 当前状态显式化 ✅ 2026-05-07

- [x] `docs/dogfood-v1.6.md` 顶部加 `## Status` 段，明确写
      "Pending — dogfood window has not yet started"
- [x] 列出维护者需要做的 4 步 (pick path / enable dreaming / schedule /
      fill table)
- [x] `README.md` Auto-dreaming 段加 dogfood pending 注解

**验收通过:** 任何读者一眼能看出 dogfood 没启动 + 知道怎么启动。

### [x] M7 — wiki-lint stale 检查文档/实现对齐 ✅ 2026-05-07

- [x] `wiki-lint/SKILL.md` 检查 5 重写: 显式分两层
      (Mechanical: lint_naive.py threshold / Semantic: skill prompt
      LLM 判断)
- [x] 不再装作 entity-aware staleness 是机械化检查

**验收通过:** SKILL.md 与 lint_naive.py 之间没有"假装机械化"的描述。

### [x] M8 — digest 计 `created` 增量 ✅ 2026-05-07 (R3)

- [x] `digest.py` 增 `recently_created` 字段 (driven by frontmatter
      `created`)，与 `recently_updated` 并列输出
- [x] `_coerce_date()` 共用辅助处理 str/date/datetime
- [x] `wiki-digest/SKILL.md` 步骤 ③ 改写：明确 created vs updated 两路
- [x] Test 15 增断言 `recently_created` 字段必须存在

### [x] M9 — wiki_dream `_apply_promote` 去重 ✅ 2026-05-07

- [x] `_TIER_OVERRIDE_KEYS` 常量 + 写入前 strip 所有 prior `tier_override*`
      行
- [x] Test 15.5 build apply-then-reapply 用例: 三个 key 都只剩一行；
      时间戳是第二次 apply 的；旧 reason 被清掉

**验收通过:** 反复 apply 不产生 frontmatter cruft。

### [x] M10 — external_plugin_run 三条安全回归测试 ✅ 2026-05-07 (R5)

- [x] **Test 10.5** — 6 个 injection 标记 (`<|im_start|>`, `<|im_end|>`,
      "IGNORE PREVIOUS", "You are now", `[[INST]]`, `[[/INST]]`) 全部被替换
      为 `[[REDACTED-INJECTION-MARKER]]`，"clean tail" 透传
- [x] **Test 10.6** — `max_output_bytes: 256` 截断 5000 byte 输出，
      frontmatter 写入 `truncated: True`
- [x] **Test 10.7** — 父进程 `OPENAI_API_KEY` / `CUSTOM_SECRET` 不通过
      `_safe_env()` 进入子进程；canary 字符串不出现在最终落盘文件中
- [x] 顺手记录: smoke 第一次跑 path 失败暴露另一个 YAML 解析 bug
      (Windows 路径 `C:\...` 里的冒号被当 dict key)。test 改用 `"..."`
      引号绕过；正解需要在 R6 之后再扩 wiki_lib 解析器。已记录到 known-gap。

---

## 🟡 LOW (可纳入 v1.8 backlog)

### [x] L11 — YAML 解析器对未支持语法显式报错 ✅ 2026-05-07 (R6)

- [x] `wiki_lib.YamlParseError` 新增；`_parse_scalar` 检测裸 `|` / `>`
      / `&anchor` / `*alias` 时 raise，引号包起来仍正常解析
- [x] `load_schema` 改成按 block 收集错误进 `_parse_errors`，不再
      整体 `except Exception: continue` 静默吞错
- [x] `schema_validate` 把每条 yaml-parse 错误前缀为 `yaml-parse:`
      surface 到最终 errors 列表
- [x] Test 12c.1 (block scalar) + 12c.2 (anchor) 两条新断言锁住

### [ ] L12 — 抽 `plugin/_OPS.md` 共享部分

- [ ] CLAUDE.md / AGENTS.md 共有的「path resolution / SCHEMA.md 权威 /
      output format / log format」抽到 `plugin/_OPS.md`
- [ ] 两个壳文件 include 共享内容，仅留 tool-specific 差异

### [ ] L13 — wiki-config set 失败时给可操作输出

- [ ] `cmd_set` 失败 (path 不存在) 时打印目标 YAML block 当前原文
- [ ] 给一个 `# TODO change me here` 锚点

---

## 进度时间轴

| 日期 | 事件 |
|------|------|
| 2026-05-07 | 审查完成；tracker 建立 |
| 2026-05-07 | HIGH-1..HIGH-4 全部关闭；MED-5/6/7/9 关闭；M8/M10 留待下一轮；LOW 进 v1.8 backlog |
| 2026-05-07 | smoke test 从 27 → 29 (新增 13.5 多项目 watcher / 15.5 dream re-apply / 16.5 dreaming init)；全过 |
| 2026-05-07 | 顺手修了 `custom_dimensions: []` 的渲染→解析→验证回归 (wiki-init) |
| 2026-05-07 | 跨 LLM 复审命中 6 项 follow-up — R1..R6 全部关闭：tests scratch dirs gitignore；dogfood-v1.6.md 改正 wiki-config 不能新建 block 的事实；M8 实装 recently_created；watcher PRD/TRD/docs 同步到 per-project PID；M10 三条安全测试全过；L11 YamlParseError 上线 |
| 2026-05-07 | smoke test 现 55 个 ok 断言；新增 Test 10.5/10.6/10.7 (external plugin 安全) + 12c.1/12c.2 (yaml 解析严格)；全过 |
| 2026-05-07 | 已知次级 bug（不阻塞）：自定义 YAML 解析器把 Windows 路径里的 `:` 当 dict key (workaround：argv 里加引号)。补到 known-gap。 |
| 2026-05-07 | 第三轮跨 LLM 复审：3 项 P2/P3 follow-up — R7 (alias guard 单独测试) / R8 (wiki-dream prose 把 "page mtimes" 改成 "frontmatter dates ingested_at/updated"，README + 标准 SKILL.md + plugin SKILL.md 都同步) / R9 (PRD 示例对齐 `watcher-{leaf}-{sha1[:8]}`)。 |
| 2026-05-07 | 第三轮 reviewer 做了 mutation testing 确认 5 条 round-2 测试都 pass-for-the-right-reason；本轮新加 12c.3 alias 测也用本地 mutation 验证 (临时去掉 guard → 12c.3 失败 → 恢复)。 |
| 2026-05-07 | smoke ok 断言 55 → **56**；准备 commit。 |

---

## 跨 LLM 复审 checklist (给下一个 reviewer)

下一轮交叉审核时建议先做:

1. `git diff` 看本轮改动是否真的解决了对应条目 (而不是只动文档)。
2. **跑 `py -3 tests/run_smoke.py`** (Windows 上裸 `python` 可能指向 Py2，
   会误报)。任何新增 test 不应被人为放宽。
3. 重点抽查:
   - `grep -ri command_template` 还剩什么
   - `wiki_watch.py` PID 是否真的多项目可并存 (Test 13.5)
   - SKILL.md 里 watch/dream/config 三段是否真的能让 standalone LLM 跑通
   - `external_plugin_run.py` 三条安全保护是否对得上 Test 10.5/10.6/10.7
   - `digest.py` 输出有没有 `recently_created` (M8)
   - 在 SCHEMA.md 写 `description: |` 然后跑 `schema_validate` 应当
     得到 `yaml-parse: ... block scalar ...` 错误
4. 没勾选的项是已知 known-gap，不是漏审。

## 已知 known-gaps (留给后续版本)

- **YAML 解析器把 Windows 路径里的 `:` 误认为 dict key 分隔符。**
  例如 argv 里裸写 `C:\Python\python.exe` 会被解析成 `{C: \Python\...}`。
  当前 workaround 是要求引号包裹：`"C:\Python\python.exe"`。彻底修需要
  解析器在 `key: value` 匹配前先排除「单字母 + `:` 后接 `\` 或 `/`」
  的盘符模式。Smoke test 10.5/10.6/10.7 已经加引号绕过，不阻塞。
- **L12** (CLAUDE.md / AGENTS.md 抽 plugin/_OPS.md 共享) 仍未做。
- **L13** (wiki-config set 失败时打印目标 block 原文) 仍未做。

## 终态校验 (2026-05-07 第三轮后)

- `py -3 tests/run_smoke.py` → 56 个 ok 断言，全过
- `py -3 scripts/build_skill_md.py --check` → in sync
- `grep -ri command_template` → 7 个文件，全部为合法的迁移说明 / 拒绝代码
  / 历史引用，没有活接口示例
- `git status -s` → 23 个修改 + 2 个新增 doc，没有 `??` 的 scratch dir 噪音
- 跨 LLM 复审三轮 (audit / round-2 / round-3) 全部清账
