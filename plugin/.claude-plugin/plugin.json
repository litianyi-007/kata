{
  "name": "kata",
  "version": "2.10.0",
  "description": "A self-evolving knowledge system for AI-paired builders. Built on Karpathy's LLM-Wiki principle, the CORE is a self-closing ingest/synthesis loop + auto-dreaming that resurfaces frozen pages when their relevance returns. Phase 1 reach (current): AI-paired engineering — compile project business semantics so agents read project conventions before they write code. Phase 2 (designed): team spec authoring + dispute resolution. Builders inherit a kata, adapt it to their project, transcend the form. 17 skills (init, import, ingest, search, graph, tier, digest, query, lint, config, dream, watch, sync, spec, session-ingest, mcp-server, federate). v1.13 SHM Phase 0+2. v1.11 session-ingest MVP. v1.12 cross-wiki federation Phase 0+1+2 shipped — kata acts as both MCP server (3 read tools + tier_distribution) AND federation client (parallel fan-out + kata:// URI scheme + per-peer timeout + wiki_id identity check + provenance preservation). Phase 3 (v2.11.0) integrates federation into spec preflight.",
  "author": {
    "name": "surebeli",
    "url": "https://github.com/surebeli"
  },
  "homepage": "https://github.com/surebeli/kata",
  "repository": "https://github.com/surebeli/kata",
  "license": "MIT",
  "keywords": [
    "workflow",
    "ai-paired-engineering",
    "project-memory",
    "builder",
    "kata",
    "wiki",
    "knowledge-base",
    "markdown",
    "obsidian",
    "multi-llm",
    "multi-machine-sync",
    "spec-management"
  ]
}
